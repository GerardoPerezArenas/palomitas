/* Generated by Together */
package es.altia.agora.interfaces.user.web.planeamiento.action.mantenimiento;

import es.altia.agora.business.escritorio.UsuarioValueObject;
import es.altia.agora.business.planeamiento.mantenimiento.persistence.MantOrganoAprobacionManager;
import es.altia.agora.business.planeamiento.mantenimiento.MantOrganoAprobacionValueObject;
import es.altia.agora.interfaces.user.web.util.ActionSession;
import es.altia.agora.interfaces.user.web.planeamiento.form.mantenimiento.MantenimientoOrganosAprobacionForm;
import es.altia.agora.interfaces.user.web.planeamiento.form.mantenimiento.MantenimientoOrganoAprobacionForm;
import org.apache.commons.logging.LogFactory;
import org.apache.commons.logging.Log;

import java.io.IOException;
import java.util.Iterator;
import java.util.Collection;
import java.util.ArrayList;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

public final class CargarOrganosAprobacionAction extends ActionSession {

    protected static Log m_Log =
            LogFactory.getLog(CargarOrganosAprobacionAction.class.getName());

    public ActionForward performSession(ActionMapping mapping, ActionForm form, HttpServletRequest request,
                                        HttpServletResponse response) throws IOException, ServletException {

        m_Log.info("CargarOrganosAprobacionAction.perform");
        String[] params = null;
        HttpSession session = request.getSession();

        if (session.getAttribute("usuario") != null){
            UsuarioValueObject usuarioVO = (UsuarioValueObject)session.getAttribute("usuario");
            params = usuarioVO.getParamsCon();
        }

        if (form == null) {
            m_Log.debug("Rellenamos el form de Mantenimiento de OrganosAprobacion");
            form = new MantenimientoOrganosAprobacionForm();
            request.setAttribute(mapping.getAttribute(), form);
        }
        MantenimientoOrganoAprobacionForm organoAprobacionForm = null;
        Iterator organosAprobacionIt = MantOrganoAprobacionManager.getInstance().findAll(params).iterator();
        MantOrganoAprobacionValueObject organoAprobacionVO = null;
        Collection organosAprobacion = new ArrayList();
        while (organosAprobacionIt.hasNext()) {
            organoAprobacionVO = (MantOrganoAprobacionValueObject) organosAprobacionIt.next();
            organoAprobacionForm = new MantenimientoOrganoAprobacionForm();
            organoAprobacionForm.setCodigo(organoAprobacionVO.getCodigo());
            organoAprobacionForm.setDescripcion(organoAprobacionVO.getDescripcion());
            organosAprobacion.add(organoAprobacionForm);
        }
        ((MantenimientoOrganosAprobacionForm) form).setOrganosAprobacion(organosAprobacion);
        session.setAttribute("MantenimientoOrganosAprobacionForm", form);
        return (mapping.findForward("default"));
    }
}