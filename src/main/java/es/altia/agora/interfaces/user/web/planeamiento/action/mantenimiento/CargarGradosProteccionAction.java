/* Generated by Together */
package es.altia.agora.interfaces.user.web.planeamiento.action.mantenimiento;

import es.altia.agora.business.escritorio.UsuarioValueObject;
import es.altia.agora.business.planeamiento.mantenimiento.persistence.MantGradoProteccionManager;
import es.altia.agora.business.planeamiento.mantenimiento.MantGradoProteccionValueObject;
import es.altia.agora.interfaces.user.web.util.ActionSession;
import es.altia.agora.interfaces.user.web.planeamiento.form.mantenimiento.MantenimientoGradosProteccionForm;
import es.altia.agora.interfaces.user.web.planeamiento.form.mantenimiento.MantenimientoGradoProteccionForm;
import org.apache.commons.logging.LogFactory;
import org.apache.commons.logging.Log;

import java.io.IOException;
import java.util.Iterator;
import java.util.Collection;
import java.util.ArrayList;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

public final class CargarGradosProteccionAction extends ActionSession {

    protected static Log m_Log =
            LogFactory.getLog(CargarGradosProteccionAction.class.getName());

    public ActionForward performSession(ActionMapping mapping, ActionForm form, HttpServletRequest request,
                                        HttpServletResponse response) throws IOException, ServletException {

        m_Log.info("CargarGradosProteccionAction.perform");
        String[] params = null;
        HttpSession session = request.getSession();

        if (session.getAttribute("usuario") != null){
            UsuarioValueObject usuarioVO = (UsuarioValueObject)session.getAttribute("usuario");
            params = usuarioVO.getParamsCon();
        }

        if (form == null) {
            m_Log.debug("Rellenamos el form de Mantenimiento de GradosProteccion");
            form = new MantenimientoGradosProteccionForm();
            request.setAttribute(mapping.getAttribute(), form);
        }
        MantenimientoGradoProteccionForm gradoProteccionForm = null;
        Iterator gradosProteccionIt = MantGradoProteccionManager.getInstance().findAll(params).iterator();
        MantGradoProteccionValueObject gradoProteccionVO = null;
        Collection gradosProteccion = new ArrayList();
        while (gradosProteccionIt.hasNext()) {
            gradoProteccionVO = (MantGradoProteccionValueObject) gradosProteccionIt.next();
            gradoProteccionForm = new MantenimientoGradoProteccionForm();
            gradoProteccionForm.setCodigo(gradoProteccionVO.getCodigo());
            gradoProteccionForm.setDescripcion(gradoProteccionVO.getDescripcion());
            gradosProteccion.add(gradoProteccionForm);
        }
        ((MantenimientoGradosProteccionForm) form).setGradosProteccion(gradosProteccion);
        session.setAttribute("MantenimientoGradosProteccionForm", form);
        return (mapping.findForward("default"));
    }
}