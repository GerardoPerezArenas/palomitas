/* Generated by Together */
package es.altia.agora.interfaces.user.web.planeamiento.action;

import es.altia.agora.interfaces.user.web.util.ActionSession;
import es.altia.agora.interfaces.user.web.planeamiento.form.ConvenioUrbanisticoForm;
import es.altia.agora.interfaces.user.web.planeamiento.form.RectificacionForm;
import org.apache.commons.logging.LogFactory;
import org.apache.commons.logging.Log;

import java.io.IOException;
import java.util.Collection;
import java.util.Iterator;
import java.util.ArrayList;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

public final class EliminarAnotacionRectificacionConvenioAction extends ActionSession {

    protected static Log m_Log =
            LogFactory.getLog(EliminarAnotacionRectificacionConvenioAction.class.getName());

    public ActionForward performSession(ActionMapping mapping, ActionForm form, HttpServletRequest request,
                                        HttpServletResponse response) throws IOException, ServletException {

        m_Log.info("EliminarAnotacionRectificacionConvenioAction.perform");
        HttpSession session = request.getSession();

        ConvenioUrbanisticoForm convenioForm = (ConvenioUrbanisticoForm)
                session.getAttribute("ConvenioUrbanisticoForm");
        Collection rectificaciones = new ArrayList();
        Iterator rectificacionesIt = convenioForm.getRectificaciones().iterator();
        RectificacionForm rectificacionForm = (RectificacionForm) form;
        RectificacionForm rectForm = null;
        String numero = null;
        while (rectificacionesIt.hasNext()) {
            rectForm = (RectificacionForm) rectificacionesIt.next();
            if (rectForm.getNumeroRectificacion().equals(rectificacionForm.getNumeroRectificacion())) {
                numero = rectForm.getNumeroRectificacion();
                break;
            }
            rectificaciones.add(rectForm);
        }
        String auxnum = null;
        //Reasignamos numeros a los posteriores
        while (rectificacionesIt.hasNext()) {
            rectForm = (RectificacionForm) rectificacionesIt.next();
            auxnum = rectForm.getNumeroRectificacion();
            rectForm.setNumeroRectificacion(numero);
            numero = auxnum;
            rectificaciones.add(rectForm);
        }

        convenioForm.setRectificaciones(rectificaciones);
        session.setAttribute("ConvenioUrbanisticoForm", convenioForm);

        return (mapping.findForward("default"));
    }
}