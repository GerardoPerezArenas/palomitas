/* Generated by Together */
package es.altia.agora.interfaces.user.web.planeamiento.action;

import es.altia.agora.interfaces.user.web.util.ActionSession;
import org.apache.commons.logging.LogFactory;
import org.apache.commons.logging.Log;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

import org.apache.struts.action.ActionForm;
import org.apache.struts.action.ActionForward;
import org.apache.struts.action.ActionMapping;

public final class SalirBienAndEspacioCatalogadoAction extends ActionSession {

    protected static Log m_Log =
            LogFactory.getLog(SalirBienAndEspacioCatalogadoAction.class.getName());

    public ActionForward performSession(ActionMapping mapping, ActionForm form, HttpServletRequest request,
                                        HttpServletResponse response) throws IOException, ServletException {

        m_Log.info("SalirBienAndEspacioCatalogadoAction.perform");
        HttpSession session = request.getSession();

        if (session.getAttribute("BienAndEspacioCatalogadoForm")!=null) {
            session.removeAttribute("BienAndEspacioCatalogadoForm");
        }
        if (session.getAttribute("registrosBien")!=null) {
            session.removeAttribute("registrosBien");
        }
        if (session.getAttribute("modoConsulta")!=null) {
            session.removeAttribute("modoConsulta");
        }
        if (session.getAttribute("criteriosBien")!=null) {
            session.removeAttribute("criteriosBien");
        }
        return (mapping.findForward("default"));
    }
}